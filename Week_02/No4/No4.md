- 串行GC：单线程执行 程序会暂停，并行GC：多线程并行执行，提高了吞吐量。

- 内存大小相等 串行GC 时间 比 并行GC的长很多。

- CMS：为老年代GC主要算法为并发标记清理，只有在初始标记和最终标记会有STW。

- G1：内存越大的情况下 G1 回收的效率越高，gc时间比其他回收器时间短，但是如果设备内存特别小的情况下 G1容易清理不及时导致退化成串行GC，甚至OOM。

  

