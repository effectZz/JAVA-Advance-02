1.将HttpServer03 代码复制到myself包里，作为后端服务，改名为 [BackendServer](https://github.com/renfufei/JAVA-000/blob/main/Week_03/nio02/src/main/java/com/renfufei/homework03/BackendServer.java), 监听 8088 端口。

2.修改了NettyServerApplication类的监听端口。

3.修改了HeaderHttpRequestFilter 的请求添加了过滤。

4.在HttpOutboundHandler 类的response header里设置了一个secret参数。

5.HeaderHttpResponseFilter 里添加了一个响应过滤。









ps：netty0基础略伤，理论知识点是看的差不多了，实战有点不懂。 

1.用的这个demo NettyServerApplication 去访问BackendServer的服务，我在浏览器访问http://localhost:8888 F12会看到2个请求，不知道原因 是因为我浏览器点击了一次和程序也调用了一次的关系么。

2. HttpOutboundHandler类的77行 。。。本来想尝试修改这个地方的路由，于是先写死个地址"http://www.baidu.com/"但是好像就没响应了 一直在挂起，这是什么原因呢。   本来是打算外面传来多个url 在RandomHttpEndpointRouter 随机出来，比如去路由到百度之类的，但是好像不太行。

![1](E:\development\geekbang\java-advance\Week_03\md图\1.PNG)